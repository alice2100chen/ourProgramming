#include <stdio.h>

int main(){
    int x = 10;
    int y = 20;
    printf("%d\n", x > y);  // 印出0，代表false
    printf("%d\n", x < y);  // 印出1，代表true
    printf("%d\n", x == y); // 印出0，代表false

    return 0;
}
