#include <stdio.h>int binary_search(int data[], int left, int right, int target){	if(left > right){		return -1;}	int middle = (int)((right-left) / 2 +left);	if(data[middle] > target){		return binary_search(data, left, middle-1, target);}	else if(data[middle] < target){		return binary_search(data, middle+1, right, target);}	else{		return middle;}}void print_array(int data[], int arr_len){	printf("[");	for(int i=0; i<arr_len; ++i){		printf("%d%s", data[i], i==arr_len-1? "":", ");}	printf("]\n");}int main(){	int data[] = {2, 3, 5, 7, 9, 11, 12, 15, 20, 22, 25};	int data_count = sizeof(data)/sizeof(int);	int target = 7;	int found_index = binary_search(data, 0, data_count-1, target);	if(found_index == -1){		printf("無法找到搜尋目標 %d\n", target);}	else{		printf("發現搜尋目標 %d , 位於陣列中第 %d 個位置\n", target, found_index);}}