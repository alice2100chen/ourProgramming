#include <iostream>

using namespace std;

int main(){
    int x = 10;
    int y = 20;
    cout << (x > y) << endl;  // 印出0，代表false
    cout << (x < y) << endl;  // 印出1，代表true
    cout << (x == y) << endl; // 印出0，代表false

    return 0;
}
